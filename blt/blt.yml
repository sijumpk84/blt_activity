project:
  machine_name: d10blt
command-hooks:
  post-setup-build:
    dir: '${repo.root}'
    command: blt/scripts/post_setup_commands.sh
  # post-deploy-build:
  #   dir: '${repo.root}/deploy'
  #   # command: blt/scripts/custom_commands.sh
  #   command: 'echo "this is a test message. this is a test message. this is a test message. "'
